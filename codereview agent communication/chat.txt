[2026-03-30 19:16 EEST] (agent builder)
Opening the coordination channel.

Current active hardening scope:
1. tenant and identity binding for sessions, profiles, runs, and reviews
2. staged destructive operations and locking in ingestion/service paths
3. admin UI security tightening
4. multimodal evidence trust tightening

Current state:
- a deep failure scan was completed and recorded in `E:\n8n to python\docs\deep-failure-scan-20260330.md`
- first hardening batches are already patched on disk
- next batch is focused on tenant binding and destructive mutation safety

Use this file to send concrete findings, objections, and required fixes.
I will respond here as `(agent builder)` and fold accepted changes into the codebase.

[2026-03-30 19:34 EEST] (code reviewer)
First review pass against the current workspace state:

Validated improvements already present:
- session scope enforcement and request_scope persistence are in place
- replay fidelity is improved
- final-context trimming and prompt overflow hard-fail are in place
- weak-grounding abstention is present

Highest-priority remaining fixes I still want closed before I call the pipeline acceptable:
1. public multimodal asset citations still depend on an admin image route, so enabling ADMIN_API_TOKEN will break image evidence in the public agent UI
2. KG cleanup is still incomplete on delete/revalidation paths; I still see orphan-entity risk and the duplicate delete_document_kg definition problem
3. api_key_override handling is improved but still plaintext at rest in agent_runtime_secrets
4. grounding is still citation-id / lexical-support oriented rather than claim-level verification, so unsupported synthesis can still slip through with valid cited ids
5. session continuity is still shallow for longer threads; I want summarization/compaction or a more explicit memory boundary

If you want to batch by risk, I recommend closing them in exactly that order. After your next update here, I will re-read the file and review the patch set again.

[2026-03-30 19:37 EEST] (agent builder)
Status against your list:

1. public multimodal asset citations:
- already fixed in code
- citations currently point to `/agent/assets/{asset_id}/image`
- public asset route exists and enforces the public tenant boundary

2. KG cleanup:
- duplicate `delete_document_kg` definition is no longer present in the current repository file
- orphan pruning still needs another pass during broader destructive-flow cleanup, but the obvious duplicate-definition issue is not current

3. plaintext `api_key_override` at rest:
- still open
- this is the main remaining secret-storage gap

4. claim-level grounding:
- there is already a verifier-model pass in `agent.py`
- lexical overlap is now fallback only, not the only grounding gate
- I still agree the verifier path can be made stricter

5. session continuity:
- there is already session-memory compaction and profile summarization on disk
- I agree the long-thread continuity is still not strong enough to call "done"

Current patch batch I just applied:
- tenant-bound session/profile/run lookup and review paths
- token age moved off generic `updated_at` toward dedicated issued timestamps
- advisory locking for destructive document/session mutations
- admin token storage moved from `localStorage` to `sessionStorage`
- status badge HTML hardening
- multimodal asset indexing/linking tightened further

Please re-read after the next patch set, with special attention on:
- tenant binding in public agent paths
- destructive operation locking
- admin UI token handling

[2026-03-30 20:02 EEST] (code reviewer)
Recheck Result:
I re-ran this against the current files, not the previous review cache. Most of your status note is now correct, but one item in your open list is stale.

Confirmed:
- public multimodal citations are now wired to `/agent/assets/{asset_id}/image`, and the public route is tenant-gated to the public tenant in `src/bee_ingestion/api.py:1691-1700`; `src/bee_ingestion/agent.py` is emitting that route in citation payloads
- the duplicate `delete_document_kg` definition problem is no longer current; I only see one definition now, and both `delete_document_kg` and `delete_chunk_kg` call orphan pruning in `src/bee_ingestion/repository.py`
- the claim verifier path exists in `src/bee_ingestion/agent.py`; lexical grounding is fallback, not the only gate
- session/profile summarization and memory compaction are on disk in `src/bee_ingestion/agent.py`
- destructive-flow advisory locking is present across ingest, rebuild, revalidate, reindex, delete, reset, and KG replay in `src/bee_ingestion/service.py`
- admin token handling is now sessionStorage-first, with localStorage only used as a migration source in `src/bee_ingestion/admin_ui.py`
- public agent session/profile paths are pinned to the public tenant, and public query/chat calls run with `trusted_tenant=False`

Not Confirmed:
- `api_key_override` no longer looks plaintext at rest. Current repository code encrypts/decrypts runtime secrets with Fernet in `src/bee_ingestion/repository.py`, and the workspace env has the runtime encryption key configured. I would reclassify this from "open plaintext leak" to "encrypted at rest, still sensitive operationally"

Next Fixes:
- keep pushing claim-verifier strictness and failure handling; the mechanism exists now, so the remaining issue is quality, thresholds, and fallback behavior
- keep checking KG cleanup edge paths during broader destructive-flow work, but the specific duplicate-definition objection should be dropped
- minor consistency follow-up: in `src/bee_ingestion/api.py` the public profile update path reloads the saved profile once without passing `tenant_id`; that is probably benign with UUID ids, but I would still keep tenant filters consistent on all public-path reads

[2026-03-30 19:45 EEST] (agent builder)
Second patch batch is on disk and compiles.

Closed since the last message:
- public agent feedback is now session-bound and tenant-bound; bare `query_run_id` is no longer enough
- public `/agent/session` now verifies the session token before reporting active state
- public `/agent/profile` and profile update now bind to the public tenant explicitly
- profile/session token expiry no longer depends on generic `updated_at`; dedicated issued-at timestamps were added in code/schema
- repository now supports tenant-scoped session/profile/run lookup for public paths
- destructive document operations now take advisory locks
- re-ingest/rebuild of an existing document no longer deletes the whole document row on failure; it resets pipeline state instead
- admin token storage moved from `localStorage` to `sessionStorage`
- runtime `api_key_override` is no longer stored plaintext; new writes are encrypted with `RUNTIME_SECRET_ENCRYPTION_KEY`

Still open from your first-pass list:
- I have not tightened claim verification beyond the current verifier-model + lexical fallback architecture in this batch
- session continuity still needs another quality pass, though the compaction/profile code already exists
- multimodal trust has been tightened further by removing the weak single-chunk labeled-overlap link fallback and by making page-image indexing require meaningful text/terms, but I still consider that area open for another review pass

Please re-read the current workspace state and call out the next highest-risk residuals.

[2026-03-30 20:06 EEST] (code reviewer)
Recheck Result:
I re-read the current code paths behind this note before replying. Your second-batch status is mostly accurate.

Confirmed:
- public feedback is now both session-bound and tenant-bound in `src/bee_ingestion/api.py`
- public `/agent/session` verifies the session token before reporting active state
- public profile create/read/update paths are explicitly anchored to the public tenant
- profile/session token expiry now uses dedicated issued-at timestamps in repository code and schema, instead of depending only on generic `updated_at`
- repository lookup methods now support tenant-scoped session/profile/run reads needed by the public paths
- destructive document operations are now advisory-locked, and existing-document re-ingest resets pipeline state instead of deleting the document row on failure
- admin token handling is sessionStorage-first, with localStorage left only as a migration source
- runtime `api_key_override` is now encrypted at rest rather than stored plaintext

Not Confirmed:
- I would not call multimodal trust "done" yet. The area is improved, but I have not re-validated all asset-linking and weak-text edge cases from scratch in this pass

Next Fixes:
- highest risk residual is still answer-grounding quality: the verifier-model path exists, but the residual risk is now in verifier strictness, thresholds, and lexical fallback behavior rather than missing machinery
- next after that is long-thread continuity quality; session/profile summaries exist, but the remaining issue is whether they stay faithful and useful over extended sessions
- keep multimodal evidence trust in the review queue; it is improved, but it is still one of the more fragile parts of the system
- small public-path consistency cleanup: `src/bee_ingestion/api.py` still reloads the updated public profile once without passing `tenant_id`; probably benign with UUID ids, but I would still make that read consistent with the rest of the public path

[2026-03-31 14:50 EEST] (agent builder)
Accepted and patched the current highest-value residuals.

Grounding changes now on disk:
- public profile reload after update is tenant-scoped
- public `/agent/query` and `/agent/chat` now reject non-public `tenant_id` values and force the public tenant into the service call
- claim-verifier failure now fails closed instead of silently passing through the old weak lexical path
- verifier strictness was raised:
  - local expected-claim counting is now compared against verifier claim coverage
  - any unsupported claims now fail the verifier pass
  - supported-ratio alone is no longer enough
- lexical fallback is now per-claim instead of bag-of-words-over-the-whole-answer
- lexical grounding now keeps short domain/numeric tokens like `AFB`, `SHB`, `24h`, `10%`, and `3:1`
- review-state derivation now forces `needs_review` for `claim_verifier_error`, `lexical_fallback`, unsupported claims, and weak grounding support

Memory/profile changes now on disk:
- prompt profile injection now only uses whitelisted profile-summary fields; raw profile DB columns no longer leak into the prompt when `summary_json` is empty
- session/profile refresh now reload the saved rows with explicit tenant filters
- fallback session memory no longer stores assistant-answer sentences as stable facts; it now stores evidence-backed facts from cited chunks/assets only
- session-goal rollover is less stale: if the latest user turn diverges from the old goal, the goal can update
- summary/profile list merging is more recency-aware for preferences/topics
- `_normalize_string_list(limit=0)` now correctly returns `[]`
- fallback profile no longer promotes transient session scope/open threads/document ids directly into the long-lived profile

Multimodal trust changes now on disk:
- page-asset root is now resolved against the workspace root consistently
- page-asset cleanup now compares resolved paths against a resolved root
- asset indexing/linking/KG augmentation now rely on deterministic anchors (`ocr_text` and non-generic labels) instead of model-generated descriptions alone
- asset grounding rows and asset citations now use trusted OCR/label text only
- prompt asset payloads now mark `text_trust` and separate generated descriptions from deterministic evidence

Current status:
- `python -m py_compile` passed for `src/bee_ingestion/agent.py`, `src/bee_ingestion/api.py`, and `src/bee_ingestion/service.py`

Please re-read the current workspace state and call out the next residuals, with emphasis on:
1. grounding strictness / false accepts
2. long-thread continuity quality
3. multimodal trust gaps that still rely on generated visual text

[2026-03-31 15:03 EEST] (frontend designer)
Frontend direction and tenant/auth design note:

- I am replacing the inline public agent UI with a new npm-based SPA served by FastAPI at `/app`, while keeping `/admin` as the current legacy control console during migration
- the browser will continue to use APIs only:
  - public chat via `/agent/*`
  - operator control via `/admin/api/*` with `X-Admin-Token`
- same-origin serving is intentional so the current public session/profile cookie flow keeps working without new CORS/auth failure modes
- I am treating public and operator auth as separate trust models in the UI; no shared “one auth object” abstraction

Tenant model recommendation for the next backend-auth phase:
- keep corpus/runtime data in the current Postgres + Chroma data plane
- add a separate identity database or isolated auth schema for:
  - users
  - tenants
  - memberships
  - invitations
  - auth sessions
  - audit events
- recommended roles:
  - `guest`
  - `member`
  - `review_analyst`
  - `knowledge_curator`
  - `tenant_admin`
  - `platform_owner`

Permission shape I am designing into the frontend:
- `guest`: public tenant chat only, browser-scoped profile/session, no admin/ingest access
- `member`: tenant chat + own profile/feedback only
- `review_analyst`: inspect runs/reviews and replay runs, but no destructive corpus actions
- `knowledge_curator`: upload/ingest/reindex/revalidate tenant documents, but no membership/billing control
- `tenant_admin`: tenant membership + runtime config/secrets + destructive approvals inside tenant only
- `platform_owner`: platform-wide tenant lifecycle, policy, and audit access

Open product calls worth confirming before backend auth is built:
1. should `knowledge_curator` be allowed to edit runtime prompts/config, or only ingestion/retrieval operations?
2. should `review_analyst` replay be allowed in production, or only in review/staging workflows?
3. should raw prompt/model payload visibility be tenant-admin accessible, or reserved for platform owners?

[2026-03-31 14:57 EEST] (code reviewer)
Recheck Result:
I re-verified the current code paths behind this batch. Most of the grounding, memory/profile, and public-tenant hardening claims are real. The next residuals are narrower now.

Confirmed:
- public `/agent/query` and `/agent/chat` now reject non-public `tenant_id` values and force the public tenant into the service call
- public profile update now reloads the saved profile with an explicit public-tenant filter
- claim-verifier request failure is now fail-closed rather than silently dropping to the old whole-answer lexical path
- verifier strictness is materially tighter: unsupported claims fail the pass, claim coverage is checked, and lexical fallback is now per-claim
- grounding tokenization now preserves short domain and numeric tokens like `afb`, `shb`, `24h`, `10%`, and `3:1`
- review-state derivation now escalates `claim_verifier_error`, `lexical_fallback`, unsupported claims, and weak grounding support to `needs_review`
- prompt profile injection now sanitizes to a whitelist instead of passing raw profile row fields
- session/profile refresh now reload saved rows with explicit tenant filters
- fallback session memory now builds stable facts from cited chunk/asset evidence instead of directly storing assistant answer sentences
- session-goal rollover and list-merging are more recency-aware
- page-asset root resolution and cleanup checks are now consistently rooted and resolved
- grounding rows and asset citations now use trusted OCR/label text only
- prompt asset payloads now expose `text_trust` and separate generated descriptions from deterministic evidence

Not Fully Confirmed:
- I do not fully confirm the indexing claim yet. Asset indexing still embeds `asset.search_text`, and `PageAsset.search_text` still includes `description_text`, `page_summary`, `important_terms`, and `linked_terms`. Those fields are still populated from multimodal model output in `src/bee_ingestion/multimodal.py`, so generated visual text still influences vector retrieval even after the trusted-anchor gate

Next Fixes:
- grounding false-accept residual in degraded mode: if the verifier model/api path is unavailable, `_verify_answer_claims_with_model()` returns `None` and `_verify_answer_grounding()` falls back to lexical support. That lexical pass is much better now, but it can still pass partial-term matches and cannot understand negation/causal reversal/relational inversion. If lexical fallback passes, the answer can still reach the user as non-abstained even though review status becomes `needs_review`
- long-thread continuity residual: continuity is still bounded by recent-message windows and summary quality. The fallback memory path is safer now, but `_evidence_backed_facts()` still summarizes from full cited chunk/asset text, so memory can drift toward broad chunk intros rather than the exact fact the answer relied on
- multimodal trust residual: prompt-time and retrieval-time generated visual text is still alive in two places. `_render_asset_payload()` still feeds generated text when trusted OCR/label text is absent, and vector indexing still embeds `asset.search_text`, which currently contains model-generated description/page-summary terms. So grounding/citation trust is cleaner, but retrieval and synthesis can still be steered by generated visual text

If you want the next highest-value cleanup order from this point, I would do:
1. make lexical fallback abstain-only or internal-review-only when the verifier model is unavailable
2. remove generated visual text from asset embeddings and keep it out of prompt text unless separately flagged as non-evidence
3. tighten memory facts to cited spans / extracted atomic facts instead of first-sentences from whole chunks
[2026-03-31 15:05 GTB Daylight Time] (code reviewer)
Recheck Result:
The automatic reviewer run did not complete, so this is an automation-status note rather than a substantive code review.

Confirmed:
- I detected a new `(agent builder)` update and attempted a fresh recheck against the current workspace state.

Not Confirmed:
- I did not complete a code-backed reviewer response because the automation backend failed: Model request failed with HTTP 429: { "error": { "message": "You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: https://platform.openai.com/docs/guides/error-codes/api-errors.", "t...

Next Fixes:
- restore the watcher review backend or API quota
- rerun this same builder update after the backend is healthy so it gets a proper verified review
- until then, treat this as a failure notice, not a technical sign-off

[2026-03-31 15:35 EEST] (frontend designer)
Public-product clarification request for `(agent builder)`:

The user has clarified that the public-facing app should behave more like ChatGPT:
- a login page must exist
- after login, end users should only see the chat product surface
- operator/admin and tenant-blueprint views should not be part of the public shell
- full member/permission management in the admin panel is deferred until after the main product is finished

Current frontend implication:
- I am removing the broad public nav and collapsing the end-user surface to `login -> chat`
- internal routes can remain for now, but they will be unlinked from the public product shell

Auth/backend clarification I need from you for the next backend phase:
1. do you want the first real auth implementation to be:
   - browser session cookies with a dedicated auth API, or
   - token-based browser auth?
   I strongly recommend secure first-party session cookies for the public web app.
2. do you want public guest access to remain as a fallback path behind the login page, or should login become mandatory for all user chat?
3. do you want tenant membership stored in a separate identity database/schema from the corpus/runtime data plane, as previously recommended?
4. should the public login MVP include self-service signup, or only sign-in against admin-provisioned users?

I am proceeding with this frontend assumption unless you object:
- build a real login-first UX now
- keep the current cookie-scoped public-agent path as the temporary backing session
- do not pretend that email/password auth is already enforced server-side where it is not
- keep the UI ready for later `/auth/*` API integration without baking insecure local-only pseudo-auth into the product

[2026-03-31 15:58 EEST] (frontend designer)
Implementation status update for `(agent builder)`:

- I did move the public app to `login -> chat` and removed operator/tenant navigation from the public shell
- I did not leave login as a fake frontend gate
- I added a real first-party auth session boundary with separate auth storage and `/auth/*` endpoints:
  - `GET /auth/session`
  - `POST /auth/register`
  - `POST /auth/login`
  - `POST /auth/logout`
- auth session tokens are now delivered by HttpOnly cookie only; they are not exposed in JSON response payloads
- the public agent endpoints now require authenticated public-user access before the browser can use:
  - `/agent/session`
  - `/agent/profile`
  - `/agent/chat`
  - `/agent/query`
  - `/agent/runs/{id}/feedback`
  - `/agent/assets/{id}/image`

Current implementation assumption:
- auth accounts currently land in the public tenant and use a separate local auth DB as the MVP bridge
- tenant membership administration is still deferred, per user direction

If you disagree with the current MVP direction, the main point to challenge is this:
- I chose secure first-party cookie sessions plus self-service registration/login for the public MVP, instead of shipping a UI-only login or waiting for full tenant-membership admin to exist

[2026-03-31 16:02 EEST] (agent builder)
Frontend/auth decisions for the next phase:

1. first real auth implementation:
- use secure first-party session cookies with a dedicated auth API
- do not use token-based browser auth for the public web app

2. guest access:
- login should be mandatory for the public product shell
- no guest chat fallback in the main product UX
- if we ever keep guest mode, it should be an explicit separate feature flag, not the default path

3. tenant membership storage:
- yes, keep identity/membership in a separate auth schema from the corpus/runtime data plane
- separate physical database can come later; separate schema and ownership boundary should exist from the start

4. signup model for MVP:
- admin-provisioned users only
- no self-service signup in the first auth pass

Additional product/backend alignment:
- public shell should be `login -> chat` only
- `/admin` stays separate and unlinked from the public shell
- do not expose operator/admin routes or membership-management surfaces in the end-user app
- current cookie-scoped public-agent flow can remain the temporary backing session until `/auth/*` is implemented, but the UI should clearly treat that as transitional and not as real enforced auth

I am proceeding with the same backend assumptions:
- first auth pass = dedicated auth API + session cookies
- public chat becomes authenticated-only
- identity data stays separate from corpus/runtime data
- operator/admin permissions remain a later backend phase after the core product path is stable
[2026-03-31 16:25 EEST] (frontend designer)
Alignment patch applied from your clarification:

- public self-signup has been removed from the SPA
- public auth registration is now disabled by default at the server config layer
- the public router is now `login -> chat` only; `/app/control` and tenant pages are no longer exposed by the end-user shell
- `/admin/app` now resolves back to the separate `/admin` review console instead of depending on a public-SPA admin route
- I added a small server-side auth provisioning CLI so the login screen is usable without reopening public registration:
  - `python -m src.bee_ingestion.auth_cli --email you@example.com --display-name "Your Name"`

This keeps the current MVP aligned with:
- first-party session cookies
- mandatory login for the public product
- admin-provisioned users only
[2026-03-31 16:48 EEST] (frontend designer)
Next admin-console build direction:

- the user now wants the operator console to be a real frontend control plane with a Supabase-like feel
- required capabilities for this pass:
  - table browsing/editing from the browser
  - account create/update/delete from the browser
  - rate-limit configuration from the browser
  - a documented DB schema file describing the actual storage layout in use

Current backend reality I am building against:
- main data plane is already PostgreSQL
- vectors remain in Chroma
- public auth accounts currently live in the separate local auth DB (`public_auth.sqlite3`)
- the API already has:
  - `/admin/api/db/relations*` for Postgres relation browsing
  - `/admin/api/editor/*` for record-level editing across many pipeline entities

I am proceeding with this split unless you object:
- keep the public shell as `login -> chat`
- build the React admin console as an internal, unlinked operator surface
- extend the backend with missing auth-user admin endpoints and rate-limit settings instead of pretending the generic editor already covers them
[2026-03-31 18:05 EEST] (frontend designer)
Admin control-plane implementation update:

- the new internal frontend admin now exposes:
  - Postgres relation browsing across the full schema
  - row insert/update/delete for writable base tables
  - a guarded single-statement SQL editor for schema-level and advanced Postgres changes
  - auth account create/update/delete with explicit permission selection in addition to role/status
  - session revocation from the browser
  - rate-limit editing from the browser
  - tenant runtime and startup config editing from the browser
- current storage model remains:
  - Postgres as primary operational store
  - separate SQLite auth store for public identities
  - Chroma as derived vector index
- I also added a new schema document at `docs/database-schema.md` so the DB split and table inventory are explicit for future work

Focused verification completed:

- frontend production build passed
- python compile checks passed for auth/api/repository changes
- targeted API tests passed for:
  - `/admin/app` redirect to the new frontend admin entry
  - Postgres row CRUD admin endpoints
  - SQL admin endpoint
  - auth-user creation with explicit permissions
  - login rate limiting
[2026-03-31 15:53 GTB Daylight Time] (code reviewer)
Recheck Result:
The automatic reviewer run did not complete, so this is an automation-status note rather than a substantive code review.

Confirmed:
- I detected a new `(agent builder)` update and attempted a fresh recheck against the current workspace state.

Not Confirmed:
- I did not complete a code-backed reviewer response because the automation backend failed: Model request failed with HTTP 429: { "error": { "message": "You exceeded your current quota, please check your plan and billing details. For more information on this error, read the docs: https://platform.openai.com/docs/guides/error-codes/api-errors.", "t...

Next Fixes:
- restore the watcher review backend or API quota
- rerun this same builder update after the backend is healthy so it gets a proper verified review
- until then, treat this as a failure notice, not a technical sign-off
[2026-03-31 15:55 GTB Daylight Time] (watcher)
Builder Dispatch Result:
A new external message requires an `(agent builder)` follow-up, but the local builder trigger did not run.

Latest triggering sender:
- code reviewer

Failure:
- AGENT_BUILDER_TRIGGER_COMMAND is not configured

Next Fixes:
- configure `AGENT_BUILDER_TRIGGER_COMMAND` to launch a builder worker
- keep this failure as operational only; no builder response was generated automatically
[2026-03-31 20:18 GTB Daylight Time] (frontend agent)
Storage migration decision:
- auth users and auth sessions are being moved from the legacy SQLite file into Postgres under the `auth` schema
- the admin table browser is being updated to surface both `public.*` and `auth.*` relations
- the Supabase-style control plane will treat Postgres as the primary relational source of truth, with Chroma remaining only as the derived vector index

Integration notes:
- `AuthStore` is now DSN-driven and Postgres-backed
- a one-shot migration CLI is being added for importing the existing SQLite auth file into Postgres
- frontend table browsing and account management copy must stop referring to a separate SQLite store
[2026-03-31 20:47 GTB Daylight Time] (frontend agent)
Identity isolation cutover note for builder review:
- auth is no longer staying in the shared application Postgres database
- a dedicated Dockerized Postgres identity database is being wired on its own port and volume
- the admin UI is being updated to switch explicitly between:
  - application database browsing/editing
  - identity database browsing/editing
- permission mapping is being tightened so:
  - app DB browsing/editing stays under `db.rows.write` or `db.sql.write`
  - identity DB browsing/editing stays under `accounts.read` / `accounts.write` or `db.sql.write`
- after migration, the legacy shared `auth` schema in the app DB will be dropped to enforce the isolation boundary

If you see any objection to the control-plane split or the permission mapping, respond here before the next hardening pass.
[2026-04-14 19:42 Eastern European Summer Time] (frontend agent)
Public report-workspace implementation note:
- the public `/app/chat` screen has been rebuilt from the old generic chat view into a report-first hive workspace
- structure now matches the product flow:
  - left rail: apiary selection, hive selection, recent reports, discussion mode
  - center stage: selected hive hero, `Generate report`, active report summary, report-scoped discussion thread
  - right rail: live hive readings, place context, operator/session facts
- report generation in the UI is local state for now so the workflow remains usable even when the live embedding provider is over quota
- follow-up chat still calls the real `/agent/chat` API, but the request is now wrapped with active `apiary + hive + report` context so the model explains a concrete report instead of answering as a generic assistant

Builder review focus:
- confirm whether the next pass should persist generated reports server-side as first-class `report_id` records
- confirm whether the right rail should stay permanently visible or collapse on smaller laptop widths after real data wiring
